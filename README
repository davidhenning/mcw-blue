Wordpress Theme mcw[blue]
=========================

mcw[blue] is a HTML 5 version of my current Wordpress Theme based on PageLines Plattform.
Plattform is very powerful but the HTML source is rather ugly with way to much markup and nested elements.
So I decided to build an entirely new theme with HTML 5 and CSS-3. 